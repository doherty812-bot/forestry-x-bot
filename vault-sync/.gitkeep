# Optional Actions sync mount (same role as obsidian/).
# Prefer committing public notes to ../obsidian/ (method 1).
# This folder stays gitignored except this file; use for local/artifact mounts only.
